version: "3.9"

services:
  backend:
    build: ./Backend
    ports:
      - "8000:8000"
    env_file:
      - ./Backend/.env
    networks:
      - hostelsync-network

  frontend:
    build: ./Frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - hostelsync-network

networks:
  hostelsync-network:
    driver: bridge
